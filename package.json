{
  "name": "express-gpio-rest-api",
  "version": "0.0.1",
  "description": "Express server script for a RESTful API to access the Raspberry Pi's GPIO pins.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Electro Cloud",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.13.4",
    "pi-gpio": "0.0.8"
  }
}
